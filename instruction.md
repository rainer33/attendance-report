I have an attendance report generator script at /app/attendance/report_generator.py that would produce a monthly report on employees’ attendance. The script is using data from /app/data/attendance.csv, /app/data/employees.json, and /app/data/leave_requests.csv to produce the report. But the attendance system admin reported that some monthly totals look wrong in a few situations: repeated check-ins can make totals look higher than expected, one employee who worked on end of the month is not reflected correctly, overtime can drop unexpectedly after leave approvals, and a missed clock-out can make employees daily totals inconsistent. There is also a case where an employee with no attendance activity in the month still needs to appear in the final report. You can see how the function in the script is called in /app/run_report.py. 

Please investigate and fix these issues. The report should return a list of dicts with employee_id (str), employee_name(str), worked_hours(float), leave_hours(float), and overtime_hours(float), sorted by employee_id. Each employee should only appear just once in the final report. Do not modify the input data or the test files and make sure that all the tests are passed.
